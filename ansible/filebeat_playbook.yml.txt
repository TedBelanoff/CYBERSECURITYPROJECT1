---
- name: Installing and Launch Filebeat
  hosts: webservers
  become: yes
  tasks:
    # Use command module
  #- name: Download filebeat .deb file
    #command: curl -L -O https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-oss-7.9.3-amd64.deb

    # Use command module
  #- name: Install filebeat .deb
    #command: sudo dpkg -i filebeat-oss-7.9.3-amd64.deb

    # Use copy module
  #- name: Drop in filebeat.yml
    #copy:
      #src: /etc/ansible/files/filebeat-config.yml
      #dest: /etc/filebeat/filebeat.yml

    # Use command module
  #- name: Enable and Configure System Module
    #command: filebeat modules enable system
#mysql nginx

    # Use command module
  #- name: Setup filebeat
    #command: filebeat setup

    # Use command module
  - name: Start filebeat service
    command: filebeat -e